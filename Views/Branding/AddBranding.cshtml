<div class="container-fluid">

    <!-- Header -->
    <div class="mb-3">
        <input type="hidden" value="@ViewBag.ProjectName" />
        <h4 class="mb-0">Create New Branding</h4>
        <p class="text-muted">Fill out the form below to create a new branding entry.</p>
    </div>

    <!-- Card -->
    <div class="card p-4" style="max-width:700px;">

        <div class="row g-3">

            <!-- AGENT -->
            <div class="col-md-6">
                <label class="form-label">AGENT *</label>
                <select class="form-control">
                    <option>Select Agent</option>
                </select>
            </div>

            <!-- STORE -->
            <div class="col-md-6">
                <label class="form-label">STORE *</label>
                <input type="text" class="form-control"
                       placeholder="Search by store name, store code, or ID..." />
            </div>

            <!-- UPLOAD PHOTO -->
            <div class="col-md-12">
                <label class="form-label">UPLOAD PHOTO *</label>

                <!-- Hidden input -->
                <input type="file" id="photoInput" style="display:none;" />

                <!-- Upload box -->
                <div id="uploadPhotoBox"
                     style="border:2px dashed #ddd; border-radius:10px; padding:20px; text-align:center; cursor:pointer;">

                    <span id="photoText" class="text-muted">
                        Choose File — No file chosen
                    </span>
                </div>
            </div>

            <!-- LATITUDE -->
            <div class="col-md-6">
                <label class="form-label">LATITUDE</label>
                <input type="text" class="form-control" placeholder="Enter latitude" />
            </div>

            <!-- LONGITUDE -->
            <div class="col-md-6">
                <label class="form-label">LONGITUDE</label>
                <input type="text" class="form-control" placeholder="Enter longitude" />
            </div>

        </div>

        <!-- Button -->
        <div class="mt-4">
            <button class="btn btn-primary">
                <i class="fas fa-plus-square me-2"></i> Create Branding
            </button>
        </div>

    </div>


</div>